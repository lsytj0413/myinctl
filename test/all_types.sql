-- Create table for all available data types
CREATE TABLE `all_types` (
    `c_bit` BIT,
    `c_tinyint` TINYINT,
    `c_boolean` BOOLEAN,
    `c_bool` BOOL,
    `c_smallint` SMALLINT,
    `c_mediumint` MEDIUMINT,
    `c_int` INT,
    `c_integer` INTEGER,
    `c_bigint` BIGINT,
    `c_decimal` DECIMAL,
    `c_dec` DEC,
    `c_fixed` FIXED,
    `c_float` FLOAT,
    `c_floatp` FLOAT(24),
    `c_floatp2` FLOAT(25),
    `c_double` DOUBLE,
    `c_double_pre` DOUBLE PRECISION,
    `c_real` REAL,
    `c_numeric` NUMERIC,
    `c_date` DATE,
    `c_time` TIME,
    `c_datetime` DATETIME,
    `c_timestamp` TIMESTAMP,
    `c_year` YEAR,
    `c_char` CHAR,
    `c_varchar` VARCHAR(10),
    `c_binary` BINARY,
    `c_varbinary` VARBINARY(10),
    `c_tinyblob` TINYBLOB,
    `c_tinytext` TINYTEXT,
    `c_blob` BLOB,
    `c_text` TEXT,
    `c_mediumblob` MEDIUMBLOB,
    `c_mediumtext` MEDIUMTEXT,
    `c_longblob` LONGBLOB,
    `c_longtext` LONGTEXT,
    `c_enum` ENUM('enum'),
    `c_set` SET('set'),
    `c_geometry` GEOMETRY,
    `c_point` POINT,
    `c_linestring` LINESTRING,
    `c_polygon` POLYGON,
    `c_multipoint` MULTIPOINT,
    `c_multilinestring` MULTILINESTRING,
    `c_multipolygon` MULTIPOLYGON,
    `c_geometrycollection` GEOMETRYCOLLECTION,
    `c_json` JSON
);